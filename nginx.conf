server {
    listen 7777;

    root  /usr/share/nginx/html;
    index /index.html;

    location /api/ {
        proxy_pass http://host.docker.internal:8080;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}